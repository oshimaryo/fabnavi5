machine:
  timezone:
    Japan
  ruby:
    version: 2.4.2
  node:
    version: stable

test:
  post:

deployment:
  production:
    branch: release
    commands:
      - bundle exec cap production deploy
  staging:
    branch: staging
    commands:
      - bundle exec cap staging deploy
